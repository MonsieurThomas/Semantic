generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        Int        @id @default(autoincrement())
  username  String     @unique
  email     String     @unique
  password  String
  documents Document[] // Relation to documents
}

model Document {
  id             Int      @id @default(autoincrement())
  date           DateTime @default(now())
  name           String
  color          String
  title          String
  url            String
  theme          String[]
  themeSize      Int[]
  page           Int
  mimeType       String
  path           String
  size           Int
  createdAt      DateTime @default(now())
  openaiResponse Json?
  rawText        String?  // Ajout du nouveau champ rawText
  userId         Int      // Foreign key
  user           User     @relation(fields: [userId], references: [id])
  elements       Json?    
}


model File {
  id        Int      @id @default(autoincrement())
  name      String
  mimeType  String
  path      String
  createdAt DateTime @default(now())
}
